#!/bin/bash

set -e

if type update-alternatives >/dev/null 2>&1; then
    # Remove previous link if it doesn't use update-alternatives
    if [ -L '/usr/bin/clash-meta' ] && [ -e '/usr/bin/clash-meta' ] && [ "$(readlink '/usr/bin/clash-meta')" != '/etc/alternatives/clash-meta' ]; then
        rm -f '/usr/bin/clash-meta'
    fi
    update-alternatives --install '/usr/bin/clash-meta' 'clash-meta' '/opt/clash-meta/clash-meta' 100 || ln -sf '/opt/clash-meta/clash-meta' '/usr/bin/clash-meta'
else
    ln -sf '/opt/clash-meta/clash-meta' '/usr/bin/clash-meta'
fi

chmod 4755 '/opt/clash-meta/chrome-sandbox' 2>/dev/null || true

chmod +sx /opt/clash-meta/resources/sidecar/mihomo 2>/dev/null || true
chmod +sx /opt/clash-meta/resources/sidecar/mihomo-alpha 2>/dev/null || true
chmod +sx /opt/clash-meta/resources/sidecar/mihomo-smart 2>/dev/null || true

if hash update-mime-database 2>/dev/null; then
    update-mime-database /usr/share/mime || true
fi

if hash update-desktop-database 2>/dev/null; then
    update-desktop-database /usr/share/applications || true
fi

# Update icon cache for GNOME/GTK environments
if hash gtk-update-icon-cache 2>/dev/null; then
    for dir in /usr/share/icons/hicolor /usr/share/icons/gnome; do
        [ -d "$dir" ] && gtk-update-icon-cache -f -t "$dir" 2>/dev/null || true
    done
fi

# Refresh GNOME Shell icon cache
if hash update-icon-caches 2>/dev/null; then
    update-icon-caches /usr/share/icons/* 2>/dev/null || true
fi
